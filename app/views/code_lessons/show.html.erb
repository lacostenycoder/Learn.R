<div class="container">
    <p>Progress: <span id='total-progress'><%= current_user.progress %></span>%</p>
    <h1><%= @lesson.title %></h1>
    <p>Lesson Topics: <%= @lesson.lesson_text %></p>
    <p class="points">Available Points: <%= @lesson.points %></p>

    <p>Instructions: <%= @lesson.question_text %></p>
    <%= content_tag(:div, class: "lesson-id", 'data-id' => @lesson.id) do %>
    <% end %>
    <div id="code_lessons_page"></div>

    <div class="canvas">
      <canvas id="lessoncanvas" height="600" width="600"></canvas>
    </div>
          <%= form_tag( @lesson, method: :get, remote: true) do |f| %>

          <%= label_tag(:answer, "Enter your answer") %>
          <%= text_area_tag :answer %>

          <%= submit_tag( "Submit Answer") %>

        <% end %>
      </div>

      <%= link_to "Reset", code_lesson_path %>
    </div>

</div>
